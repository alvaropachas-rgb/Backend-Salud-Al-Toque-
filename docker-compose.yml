services:

  postgres:
    image: postgres:16-alpine
    container_name: sss001-db

    ports:
      - "5434:5432"

    environment:
      POSTGRES_DB: sss001
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: